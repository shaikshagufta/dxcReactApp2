/* const name = 'yoshi';

console.log(name); */
 
const greet =(name) =>{// taking a function with name greet and take in the parameter name
    console.log(`hello ${name}`)//`hello${name}`- template string; ${name}- variables declaration in it 
}

greet ('shagufta');
greet('team4')
